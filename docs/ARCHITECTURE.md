# 🏗️ CryptoWatch - 系统架构文档

## 📋 目录

- [系统概述](#系统概述)
- [架构设计](#架构设计)
- [核心组件](#核心组件)
- [数据流](#数据流)
- [技术决策](#技术决策)

---

## 🎯 系统概述

CryptoWatch 采用**三层架构**设计,实现了数据收集、智能分析和报告生成的完整闭环。

### 设计原则

1. **模块化**: 每个Agent独立封装,职责单一
2. **可扩展**: 易于添加新的数据源和分析Agent
3. **松耦合**: 组件间通过标准接口通信
4. **可测试**: 每个模块可独立测试

---

## 🏛️ 架构设计

### 整体架构
```
┌─────────────────────────────────────────────────────────────────┐
│                      CryptoWatch System                      │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
        ┌────────────────────────────────────────────┐
        │          主系统 (CryptoWatch)            │
        │  - 初始化所有组件                           │
        │  - 协调整体流程                             │
        │  - 提供用户接口                             │
        └────────────────────────────────────────────┘
                                 │
                 ┌───────────────┼───────────────┐
                 ▼               ▼               ▼
        ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
        │   数据层    │  │   分析层    │  │   输出层    │
        └─────────────┘  └─────────────┘  └─────────────┘
```

### 三层架构详解

#### 1️⃣ 数据层 (Data Layer)

**职责**: 从各种数据源收集原始数据
```
DataAggregator (数据聚合器)
    │
    ├── Web3DataAgent
    │   └── CoinGecko API
    │       ├── 价格数据
    │       ├── 市值数据
    │       └── 交易量数据
    │
    ├── NewsSearchAgent
    │   └── Tavily API
    │       └── 新闻搜索
    │
    └── SocialSentimentAgent
        └── 情绪分析
            ├── 正面/负面占比
            ├── 热门关键词
            └── 主要观点
```

**数据格式**:
```python
{
    "topic": "Ethereum",
    "timestamp": "2026-01-18T10:00:00",
    "data_sources": {
        "web3": {...},
        "news": [...],
        "social": {...}
    }
}
```

#### 2️⃣ 分析层 (Analysis Layer)

**职责**: 使用LLM对数据进行深度分析
```
AnalysisCoordinator (分析协调器)
    │
    ├── MarketAnalyst (市场分析专家)
    │   ├── 价格趋势分析
    │   ├── 交易活跃度评估
    │   └── 市场健康度判断
    │
    ├── SentimentAnalyst (舆情分析专家)
    │   ├── 公众情绪评估
    │   ├── 舆情风险识别
    │   └── 关注焦点分析
    │
    └── RiskAnalyst (风险分析专家)
        ├── 风险等级评定
        ├── 风险因素识别
        └── 应对建议生成
```

**分析流程**:
```
原始数据 → 专家分析 → 综合结论
    │          │            │
    ▼          ▼            ▼
 价格/舆情   AI推理     报告生成
```

#### 3️⃣ 输出层 (Output Layer)

**职责**: 生成和保存分析报告
```
ReportAgent (报告生成器)
    │
    ├── 数据整合
    ├── 内容生成 (LLM)
    ├── 格式化 (Markdown)
    └── 文件保存
```

---

## 🔧 核心组件

### 1. Web3DataAgent

**位置**: `web3_data_agent.py`

**功能**:
- 连接CoinGecko API
- 获取加密货币实时数据
- 提供市场概览功能

**关键方法**:
```python
get_crypto_price(coin_id)      # 获取价格
get_trending_coins(limit)       # 热门币种
get_defi_market_data()         # DeFi数据
analyze_coin(coin_id)          # 币种分析
get_market_overview()          # 市场概览
```

**数据输出**:
```python
{
    "coin": "ethereum",
    "price": 3297.85,
    "change_24h": 0.42,
    "market_cap": 398252096796,
    "volume_24h": 11429981257,
    "timestamp": "2026-01-18T10:00:00"
}
```

### 2. NewsSearchAgent

**位置**: `data_agents.py`

**功能**:
- 搜索相关新闻
- 支持Tavily API
- 提供模拟数据(测试用)

**关键方法**:
```python
search_news(query, max_results)  # 搜索新闻
_mock_news_data(query)           # 模拟数据
```

### 3. SocialSentimentAgent

**位置**: `data_agents.py`

**功能**:
- 分析社交媒体情绪
- 提取热门关键词
- 识别主要观点

**输出格式**:
```python
{
    "overall_sentiment": "积极",
    "positive_ratio": 0.65,
    "negative_ratio": 0.10,
    "trending_keywords": ["创新", "增长"],
    "key_opinions": [...]
}
```

### 4. MarketAnalyst

**位置**: `analysis_coordinator.py`

**继承**: `ForumAgent`

**分析维度**:
1. 价格趋势
2. 交易活跃度
3. 市场健康度

**Prompt模板**:
```python
f"""你是市场分析专家。

市场数据:
- 价格: ${price}
- 24h变化: {change}%
- 市值: ${market_cap}
- 交易量: ${volume}

请分析:
1. 价格趋势
2. 交易活跃度
3. 市场健康度
"""
```

### 5. SentimentAnalyst & RiskAnalyst

**类似结构**, 各自专注不同分析维度

---

## 🌊 数据流

### 完整分析流程
```
用户输入
    │
    ▼
┌─────────────────────┐
│ CryptoWatch.analyze()
└─────────────────────┘
    │
    ▼
┌─────────────────────────────┐
│ 阶段1: 数据收集              │
│ DataAggregator.collect()    │
└─────────────────────────────┘
    │
    ├── Web3DataAgent → 价格数据
    ├── NewsSearchAgent → 新闻数据
    └── SocialAgent → 社交数据
    │
    ▼
┌─────────────────────────────┐
│ 阶段2: 专家分析              │
│ AnalysisCoordinator.analyze()│
└─────────────────────────────┘
    │
    ├── MarketAnalyst → 市场分析
    ├── SentimentAnalyst → 舆情分析
    └── RiskAnalyst → 风险评估
    │
    ▼
┌─────────────────────────────┐
│ 阶段3: 报告生成              │
│ ReportAgent.generate()      │
└─────────────────────────────┘
    │
    ▼
Markdown报告文件
```

### 数据传递
```python
# Step 1: 收集
raw_data = {
    "topic": topic,
    "data_sources": {...}
}

# Step 2: 分析
analyses = {
    "market": market_analysis,
    "sentiment": sentiment_analysis,
    "risk": risk_assessment
}

# Step 3: 报告
report_data = {
    "raw_data": raw_data,
    "expert_analyses": analyses
}
```

---

## 💡 技术决策

### 为什么选择Multi-Agent架构?

| 传统方案 | Multi-Agent方案 |
|---------|----------------|
| 单一LLM处理所有任务 | 专业Agent各司其职 |
| 分析质量依赖Prompt | 专家视角更专业 |
| 难以扩展新功能 | 添加新Agent即可 |
| 错误难以定位 | 模块化便于调试 |

### 为什么使用DeepSeek?

1. ✅ **成本效益**: API价格低廉
2. ✅ **性能优秀**: 推理能力强
3. ✅ **OpenAI兼容**: 易于切换
4. ✅ **中文友好**: 适合中文分析

### 为什么三层架构?
```
关注点分离 (Separation of Concerns)
    │
    ├── 数据层: 只管数据收集
    ├── 分析层: 只管数据分析
    └── 输出层: 只管结果呈现
    
优点:
✅ 职责清晰
✅ 易于测试
✅ 便于维护
✅ 支持并行开发
```

---

## 🔄 扩展点

### 添加新数据源
```python
class TwitterAgent:
    def search_tweets(self, query):
        # 实现Twitter搜索
        pass

# 在DataAggregator中注册
self.twitter_agent = TwitterAgent()
```

### 添加新分析Agent
```python
class TechnicalAnalyst(ForumAgent):
    def __init__(self):
        super().__init__(
            name="TechAnalyst",
            role="技术分析专家",
            perspective="关注技术指标、图表形态"
        )
```

### 添加新输出格式
```python
class PDFReportAgent:
    def generate_pdf(self, data):
        # 生成PDF报告
        pass
```

---

## 🐛 调试与监控

### 日志系统

每个Agent都有独立的日志输出:
```python
print(f"✅ {self.name} 已启动")
print(f"📊 {self.name} 正在分析...")
print(f"✅ {self.name} 分析完成")
```

### 错误处理
```python
try:
    data = api_call()
except Exception as e:
    print(f"❌ 错误: {e}")
    return fallback_data()
```

---

## 📊 性能优化

### 当前性能

- 数据收集: ~3-5秒
- AI分析: ~15-20秒
- 报告生成: ~2-3秒
- **总计**: ~20-28秒

### 优化方向

1. **并行处理**: 数据收集并行化
2. **缓存机制**: 缓存API结果
3. **流式输出**: 实时显示分析进度
4. **批量处理**: 一次分析多个项目

---

## 🔐 安全考虑

1. **API密钥**: 使用`.env`文件管理
2. **输入验证**: 检查用户输入
3. **错误处理**: 优雅地处理异常
4. **数据隐私**: 不存储敏感信息

---

## 📚 参考资料

- [LangGraph文档](https://langchain-ai.github.io/langgraph/)
- [OpenAI API文档](https://platform.openai.com/docs)
- [CoinGecko API文档](https://www.coingecko.com/api/documentation)
- [Multi-Agent系统设计模式](https://www.patterns.dev/)

---

**最后更新**: 2026-01-18  
**版本**: 1.0.0